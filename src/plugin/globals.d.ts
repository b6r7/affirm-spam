/**
 * Build-injected UI HTML (inlined by scripts/build-plugin-code.mjs).
 * Type-only; no runtime declaration. Ensures code.ts can reference __html__ without emitting any declaration.
 */
declare const __html__: string;
